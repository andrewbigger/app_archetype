image: ruby:2.4.0

pipelines:
  branches:
    master:
      - step:
          name: Build and Test
          caches:
            - bundler
          script:
            - bundle install --path .bundle
            - bundle exec rake
          deployment: production

definitions:
  caches:
    bundler: .bundle
